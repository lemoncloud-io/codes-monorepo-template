# refactor-simply

> simply to replace the `ai` as relaying function.

ex) `ai.models.generateContent()` -> `aiGenerateContent()`

but, `보안`이 약하고, 다른 사람들의 도용의 위험이 있음!!

- 그래서 `nonce`를 이용해서 코드 바꿔치기?! 흠.. 맘에 안듬!
